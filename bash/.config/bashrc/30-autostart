# -----------------------------------------------------
# AUTOSTART
# -----------------------------------------------------

# -----------------------------------------------------
# Fastfetch
# -----------------------------------------------------
if [[ $(tty) == *"pts"* ]]; then
    fastfetch
fi

# -----------------------------------------------------
# ble.sh - Attach command line editor
# -----------------------------------------------------
[[ ${BLE_VERSION-} ]] && ble-attach
